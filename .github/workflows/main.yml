name: FTP Transfer
on: push
jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Get latest code
      uses: actions/checkout@v4
    - name: Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.6
      with:
        server: ftp.sethtipton.com
        username: sethtipt
        password: 9u!yKBs2E9y
        server-dir: public_html/belltowerbrewing/
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          **/wp-content/plugins/**
